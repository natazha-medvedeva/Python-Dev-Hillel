# 3. реализовать разложение числа на степени простых множителей (ввод через input, выход по 0)
# (простое число - делится только на себя и 1)
# вход:
# 456
# 0
# вывод:
# 2^3 * 3 * 19

n = int(input())
while n!=0: # выход по 0
    L = [] # пустой список для записи простых множителей
    i = 2 # начинаем с простого множетеля 2
    while n != 1: # выполняем, пока число не снизилось до 1
        while n%i == 0: # выполняем, для каждого множителя, пока остаток от деления равен 0
            L.append(i) # добавление множителя в список
            n = n//i # уменьшение числа
        i = i + 1 # когда остаток от деления не равен 0, то проверяем следующий множитель

    V = [] # новый список для элементов, которые нужны на вывод
    for x in sorted(list(set(L))): # для каждого элемента отсортированого списка из множества элементов списка
        if L.count(x) > 1: # когда степень больше 1
            V.append(x)
            V.append('^')
            V.append(L.count(x))
            V.append(' * ')
        else: # когда степень 1
            V.append(x)
            V.append(' * ')
    for i in V[:-1]:
        print (i, end = '')
    print()
    n = int(input()) # ввод следующего числа
