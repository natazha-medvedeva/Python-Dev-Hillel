# 2. даны два словаря
# a = {'a': 1, 'b': 4, 't': 67}
# b = {'c': 4, 'e': 1, 'a': 4, 't': 7, 'y': 11}
# 2.1 найти ключи которые есть в обоих словарях
# 2.2 найти ключи которые есть только во 2м словаре, но нет в 1м
# 2.3 объединить словари в один, так чтоб числа при одинаковых ключах суммировались

a = {'a': 1, 'b': 4, 't': 67}
b = {'c': 4, 'e': 1, 'a': 4, 't': 7, 'y': 11}

# Находим ключи, которые есть в обоих словарях
a_key = [key for key in a] # ключи из 1-го словаря
b_key = [key for key in b] # ключи из 2-го словаря
a_and_b_key = set(b_key) & set(a_key)# пересечение множеств
print('Ключи, которые есть в обоих словарях:', a_and_b_key)

# Находим ключи, которые есть только во 2м словаре, но нет в 1м
b_only_key = set(b_key) - set(a_key) # разность множеств
print('Ключи, которые есть только во 2м словаре, но нет в 1м', b_only_key)

# Объединение словарей в один, так чтоб числа при одинаковых ключах суммировались
c = {} # новый словарь
for x in (set(a_key) - set(b_key)): # ключи из 1-го словаря, которых нет во 2-ом
    c[x] = a[x] # добавляем ключ и значение в новый словарь без изменений
for x in (set(b_key) - set(a_key)): # ключи из 2-го словаря, которых нет во 1-ом
    c[x] = b[x] # добавляем ключ и значение в новый словарь без изменений
for x in (set(b_key) & set(a_key)): # ключи из 1-го и 2-го словарей
    c[x] = b[x] + a[x] # добавляем ключ и суммируем значение
print('Объединеные словари в один, так что числа при одинаковых ключах просуммировались:')
print(c)
